import {
    createTheme,
} from "@nextui-org/react";

const theme = createTheme({
    type: "light",
    theme: {
        colors: {
            primary: '#fcbf17',
            link: '#5E1DAD',
            main: "#0b032d", 
            white: "#ffffff"
        },
    }
})

export default theme